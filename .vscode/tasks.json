{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Test",
            "type": "shell",
            "command": "pip install -U . && pytest",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}